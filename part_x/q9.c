/* Write several lines to stdout using puts (appends newline). */
#include <stdio.h>
#include <stdlib.h>

int main(void) {
    puts("Line one via puts.");
    puts("Line two via puts.");
    puts("Line three via puts.");
    return EXIT_SUCCESS;
}